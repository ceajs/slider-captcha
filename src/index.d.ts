/**
 * Recognize slide from base64 encoded string slide-captca image
 * @param bigImage slider background image -> base64 encoded string
 * @param smallImage slider image -> base64 encoded string
 * @param n alias for test purpose
 * @return Percentage of moving slider
 */
export async function findMovePercent(
  bigImage: string,
  smallImage: string,
  n?: string
): Promise<number | void> {}
